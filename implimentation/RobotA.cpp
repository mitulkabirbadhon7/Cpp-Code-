#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int h,b;
    cin >> h >> b;
    if(h>b){
    	cout<<h-b<<"\n";
    }
    else cout<<"0\n";

    return 0;
}